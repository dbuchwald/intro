{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Async Basic - Run the main program",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/main.js"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Async Basic - Run all Jest tests",
      "program": "${workspaceFolder}/node_modules/jest/bin/jest",
      "cwd": "${workspaceFolder}",
      "args": ["--runInBand", "--coverage"],
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}
